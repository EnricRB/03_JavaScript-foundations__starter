/* 
    1. Declare a variable using let with any name you like, but without a value.
    2. On a separate line, assign the variable the String: "Moo!"
    3. Log out this variable to the terminal using console.log
    
    Repeat the above steps but try using a const variable. Comment this new part of code and explain what is happening.
*/

//let
// 1.
let Variable1;

// 2.
Variable1 = "Moo!";

// 3.
console.log(Variable1);

//const
// 1.
// const Variable2;  // This will cause an error.

// 2.
const Variable2 = "Moo!";

// 3.
console.log(Variable2);

/*
- `let` lets you declare a variable without a value at first and then assign one later.
- `const` requires you to give it a value when you declare it, and you can't change it later.
- If you try to declare a `const` without a value, it gives an error because it needs a value from the start.
*/
